# pnpm 默认的 isolated（symlink）布局会导致 jest 无法解析 transform 包：
#   Validation Error: Module ts-jest in the transform option was not found
# 实测：同样的 jest 30 + ts-jest 29 在 npm/hoisted 布局下解析正常，
# 在 pnpm isolated 布局下必然失败（与 jest、ts-jest 版本无关）。
# 根因与处置详见 docs/规范/踩坑日志.md 第 [36] 条。
node-linker=hoisted
